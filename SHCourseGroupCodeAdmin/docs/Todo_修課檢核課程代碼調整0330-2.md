# 任務名稱
修課檢核課程代碼 - 不計學分學分數顯示調整

# 目標
當 data.NotIncludedInCredit = "1"（不計學分）時，
輸出 Excel「學分數」欄位為 0。

⚠️ 其他邏輯不可變更（包含 TryParseCredit、欄位順序、資料來源）

---

# 修改檔案
C:\ischoolProject\ScoreReportDAL\SHCourseGroupCodeAdmin\UIForm\frmCheckSCAttendCourseCode.cs

---

# 修改位置
方法：_bgWorker_DoWork  
區塊：SCAttendCodeChkInfoHasDataList 寫入 Excel（工作表：檢查修課學生課程代碼）

---

# 修改前程式碼

if (TryParseCredit(data.Credit, out double creditValue))
{
    wstSC.Cells[rowIdx, GetColIndex("學分數")].PutValue(creditValue);
}
else
{
    wstSC.Cells[rowIdx, GetColIndex("學分數")].PutValue("");
}

---

# 修改後程式碼

// 不計學分 → 學分數強制為 0
if (data.NotIncludedInCredit == "1")
{
    wstSC.Cells[rowIdx, GetColIndex("學分數")].PutValue(0);
}
else if (TryParseCredit(data.Credit, out double creditValue))
{
    wstSC.Cells[rowIdx, GetColIndex("學分數")].PutValue(creditValue);
}
else
{
    wstSC.Cells[rowIdx, GetColIndex("學分數")].PutValue("");
}

---

# 驗證項目（必做）

1️⃣ 不計學分（NotIncludedInCredit = 1） → Excel 顯示 0  
2️⃣ 一般學分 → 正常顯示原本數值  
3️⃣ 無法解析學分 → 維持空白  
4️⃣ 不影響：
   - 節數欄位
   - 課程代碼
   - 課規比對邏輯

---

# 影響範圍

✔ 僅影響 Excel 輸出顯示  
✔ 不影響資料來源  
✔ 不影響檢核邏輯  

---

# 注意事項

⚠️ 不可修改以下內容：
- TryParseCredit()
- data.Credit 原始值
- SQL 查詢邏輯
- SCAttendCodeChkInfoHasDataList 分類邏輯

---

# 修改完成紀錄

檔案：修課檢核課程代碼調整0330.md

建議內容：

## 修課檢核課程代碼調整0330

### 調整項目
- 當課程為不計學分（NotIncludedInCredit = 1），
  Excel 輸出學分數改為 0

### 調整原因
- 統一輸出格式，避免不計學分仍顯示原學分數造成誤判

### 影響範圍
- 僅影響 Excel 顯示，不影響資料計算

### 測試結果
- 不計學分資料正確顯示為 0
- 一般資料維持原本行為
