# 修課檢核課程代碼調整 0304 — 變更紀錄

## 修改檔案
- `frmCheckSCAttendCourseCode.cs`

## 變更摘要
- Pivot 來源改採 **Named Range**（`ptSrc_CheckSCAttendCourseCode`）
- 程式在寫入資料後，動態更新 `Names.RefersTo` 指向 `='檢查修課學生課程代碼'!A1:U{endRow}`（依實際最後資料列）
- 不再使用 `PivotTable.ChangeDataSource(...)` 變更資料來源；Pivot 在樣板中固定指向上述 Named Range
- Pivot 的 RefreshData / CalculateData / DisableSubtotalsForField（部定校訂）行為維持不變
- 保留既有 `GetLastDataRowByColumnA(wstSC, headerRowIndex: 0)` 以 A 欄判斷最後資料列；若 `lastRow < 1` 則直接 return，避免 Pivot 指到空範圍

## 驗證方式與結果
- 編譯：專案建置成功
- 行為：輸出 Excel 中 Named Range `ptSrc_CheckSCAttendCourseCode` 的 RefersTo 為 `='檢查修課學生課程代碼'!A1:U{實際最後列}`，樞紐分析表會依資料筆數正確更新統計結果
