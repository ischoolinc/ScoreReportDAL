# 任務名稱
修課檢核課程代碼 SQL 條件調整

# 目標
調整 DataAccess.cs 中 GetGetStudentCourseInfoBySchoolYearSemester 的 SQL，
移除以下 3 類過濾條件：
1. 課程代碼長度限制
2. 不計學分限制
3. 不需評分 + 9D 特殊條件

⚠️ 注意：其他邏輯一律不可變更（包含 SELECT、JOIN、C# 判斷）

---

# 修改檔案
C:\ischoolProject\ScoreReportDAL\SHCourseGroupCodeAdmin\DAO\DataAccess.cs

---

# 修改方法
GetGetStudentCourseInfoBySchoolYearSemester

---

# 修改內容

## 1️⃣ 移除課程代碼長度限制

【刪除】
AND char_length(COALESCE(sc_attend.subject_code,'')) = 23

---

## 2️⃣ 移除不計學分條件

【刪除】
AND COALESCE(course.not_included_in_credit,'0') = '0'

---

## 3️⃣ 移除不需評分 + 9D 特殊條件

【整段刪除】
AND (
      COALESCE(course.not_included_in_calc,'0') = '0'
   OR (
         COALESCE(course.not_included_in_calc,'0') = '1'
         AND upper(substring(sc_attend.subject_code from 17 for 2)) = '9D'
      )
)

---

# 修改後 WHERE 應為

WHERE   
 student.status IN (1,2)
 AND class.grade_year IN({0})
 AND course.school_year = {1}
 AND course.semester = {2}

---

# 驗證項目（必做）

1️⃣ SQL 可正常執行
2️⃣ 回傳資料筆數「應增加」（因為放寬條件）
3️⃣ subject_code 可為空或非23碼仍可取得
4️⃣ not_included_in_credit = 1 資料會出現
5️⃣ not_included_in_calc = 1 資料會出現（不限9D）

---

# 不可變更範圍（重要）

❌ 不可修改 SELECT 欄位
❌ 不可修改 JOIN 條件
❌ 不可修改 C# errItem 判斷邏輯
❌ 不可修改 CheckCreditPass()
❌ 不可修改 GraduationPlan 比對

---

# 風險說明

1️⃣ 資料量會增加（效能需觀察）
2️⃣ 可能出現：
   - 無課程代碼
   - 不計學分
   - 不需評分
   → 這些將交由後續 C# 檢核

---

# 修改完成紀錄

檔案：Todo_修課檢核課程代碼調整0330-1.md

內容建議填寫：

## 修課檢核課程代碼調整0330

### 調整項目
- 移除課程代碼長度限制（23碼）
- 移除不計學分過濾條件
- 移除不需評分 + 9D 特殊邏輯

### 調整原因
- 改為由 C# 統一檢核，不在 SQL 層過濾
- 避免資料在前段被過濾導致檢核不完整

### 影響範圍
- GetGetStudentCourseInfoBySchoolYearSemester 查詢結果筆數增加
- 檢核資料範圍擴大

### 測試結果
- 已完成修改：於 `GetGetStudentCourseInfoBySchoolYearSemester` 移除 SQL 層 3 類過濾條件
  （課程代碼長度 23 / 不計學分過濾 / 不需評分+9D 特殊邏輯），WHERE 後續僅保留狀態/年級/學年度/學期條件。
- 本環境未連接資料庫執行實測，因此「SQL 正常執行」與「筆數增加/各類資料可取得」仍需在實際 DB 環境驗證。
