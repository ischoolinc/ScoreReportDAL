# Todo.md — 加入解析「特殊需求領域」(chkGPlanInfo.SubjectSpecNameList)

## 目標
- 在 `chkGPlanInfo` 解析課程規劃表科目時，新增蒐集「特殊需求領域」科目的清單：`SubjectSpecNameList`
- 條件：當 `elm.Attribute("科目屬性")?.Value == "特殊需求領域"`，將該科目的 **科目名稱+級別 key**（`SubjectName + "_" + Level`）加入清單
- 修改完成後，將變更摘要記錄到：`修課檢核課程代碼調整0210.md`

---

## 範圍與檔案
- 主要修改檔案：`chkGPlanInfo.cs`（類別：`chkGPlanInfo`）
- 新增/更新紀錄檔：`修課檢核課程代碼調整0210.md`

---

## Step 1 — 新增欄位 SubjectSpecNameList
在 `chkGPlanInfo` 類別中，靠近既有的 `SubjectNameList` 定義處新增：

- `public List<string> SubjectSpecNameList = new List<string>();`
- 註解建議：`// 特殊需求領域：科目名稱_級別(key) 清單`

✅ 驗收：
- 專案可編譯
- 其他引用 `chkGPlanInfo` 的程式碼不需修改（新增欄位不破壞相容性）

---

## Step 2 — ParseSubjectDict() 初始化清單
在 `ParseSubjectDict()` 進入點（你清空 `SubjectXMLDict/SubjectDict/SubjectNameList` 的地方）補上：

- `SubjectSpecNameList.Clear();`

✅ 驗收：
- 重複呼叫 `ParseSubjectDict()` 不會累積舊資料

---

## Step 3 — 解析 Subject，遇到「特殊需求領域」就加入清單
在 `foreach (XElement elm in ContentXML.Elements("Subject"))` 迴圈內：
1. 取得科目名稱與級別（維持現有邏輯）
2. 組合 key：`key = SubjectName + "_" + Level`
3. 讀取科目屬性：`elm.Attribute("科目屬性")?.Value ?? ""`
4. 若等於 `"特殊需求領域"`：
   - 若 `SubjectSpecNameList` 不含該 key，則加入

### 實作要點
- 使用 null-safe 存取：`elm.Attribute("科目屬性")?.Value`
- 去重：`if (!SubjectSpecNameList.Contains(key)) SubjectSpecNameList.Add(key);`
- 建議把這段放在建立 `key` 後、寫入 `SubjectXMLDict/SubjectDict` 前後都可，但需確保 key 已可用

✅ 驗收：
- 課規 XML 中科目屬性為「特殊需求領域」的 Subject，都能被收集
- `SubjectSpecNameList` 內容是 `SubjectName_Level` 形式，與既有 `SubjectDict` key 一致

---

## Step 4 — 最小測試（本機即可）
準備一段最小 XML（可用單元測試或臨時程式）：

- 至少 2 筆 Subject：
  - A：`科目屬性="特殊需求領域"`、`SubjectName="..."`、`Level="1"`
  - B：`科目屬性="一般科目"` 或不含 `科目屬性`

測試流程：
1. 建立 `chkGPlanInfo`，設定 `ContentXML`
2. 呼叫 `ParseSubjectDict()`
3. 驗證：
   - `SubjectSpecNameList` 含 `A` 的 `SubjectName_Level`
   - 不含 `B`
   - 重複呼叫 `ParseSubjectDict()` 不會重複累加

---

## Step 5 — 更新變更紀錄：修課檢核課程代碼調整0210.md
在檔案中新增一段紀錄（建議附上）：
- 修改檔案與位置
- 新增欄位：`SubjectSpecNameList`
- 新增解析規則：`科目屬性 == 特殊需求領域` → 加入 key
- 測試方式與結果

✅ 驗收：
- `修課檢核課程代碼調整0210.md` 有完整變更摘要

---

## 交付物
- [ ] `chkGPlanInfo.cs` 完成：新增 `SubjectSpecNameList` + 解析邏輯
- [ ] `修課檢核課程代碼調整0210.md` 完成：新增本次修改紀錄
