# 修課檢核課程代碼調整0226

## 修改日期
2026-02-26

## 修改檔案
- `Program.cs`

---

## 修改內容

### 1. 註解移除入口：檢查班級群科班設定
**位置**：`Program.cs` 行 110-119

**修改方式**：
- 將整個功能註冊區塊以 `//` 註解
- 加入說明註解：
  - `// 2026/02/26 客服會議討論決定移除啟動功能`
  - `// 原因：功能整併與路徑調整`

**影響**：
- 功能入口不再顯示於選單
- 功能程式碼保留（僅註解），可於未來需要時恢復

---

### 2. 註解移除入口：開課檢核課程代碼
**位置**：`Program.cs` 行 122-131

**修改方式**：
- 將整個功能註冊區塊以 `//` 註解
- 加入說明註解：
  - `// 2026/02/26 客服會議討論決定移除啟動功能`
  - `// 原因：功能整併與路徑調整`

**影響**：
- 功能入口不再顯示於選單
- 功能程式碼保留（僅註解），可於未來需要時恢復

---

### 3. 註解移除入口：學期成績檢核課程代碼
**位置**：`Program.cs` 行 185-194

**修改方式**：
- 將整個功能註冊區塊以 `//` 註解
- 加入說明註解：
  - `// 2026/02/26 客服會議討論決定移除啟動功能`
  - `// 原因：功能整併與路徑調整`

**影響**：
- 功能入口不再顯示於選單
- 功能程式碼保留（僅註解），可於未來需要時恢復

---

### 4. 路徑調整：修課檢核課程代碼
**位置**：`Program.cs` 行 169-178

**原路徑**：
```
教務作業 > 課程代碼 > 資料檢查 > 修課檢核課程代碼
```

**新路徑**：
```
教務作業 > 批次作業/檢視 > 成績作業 > 修課檢核課程代碼
```

**修改方式**：
- 調整 `RoleAclSource.Instance` 路徑：`["教務作業"]["課程代碼"]` → `["教務作業"]["批次作業/檢視"]["成績作業"]`
- 調整 `MotherForm.RibbonBarItems` 路徑：`["教務作業", "課程代碼"]["資料檢查"]` → `["教務作業", "批次作業/檢視"]["成績作業"]`
- 加入說明註解：`// 2026/02/26 客服會議決議調整功能路徑`
- **不修改** handler、form、class 等程式碼

**影響**：
- 功能仍可正常開啟
- 功能顯示位置變更為新路徑

---

## 修改原則

### 僅修改 Program.cs
- ✅ 只允許修改 `Program.cs`
- ✅ 不得修改任何 Form、Business Logic、其他檔案

### 移除方式
- ✅ 「移除」一律採**程式註解**方式（不可刪除程式碼）
- ✅ 每段被註解移除的功能，必須加上同一套說明註解

### 路徑調整
- ✅ 只改「路徑參數」；不改變 handler / form / class
- ✅ 必須達成路徑層級為：
  - 第一層：教務作業
  - 第二層：批次作業/檢視
  - 第三層：成績作業
  - 功能名稱：修課檢核課程代碼

---

## 回歸檢查

### 編譯與功能
- [x] 專案可正常 Build（無編譯錯誤）
- [x] 上述 3 個功能入口已不再顯示（被註解）
- [x] `修課檢核課程代碼` 仍可正常開啟
- [x] `修課檢核課程代碼` 顯示位置正確：教務作業 > 批次作業/檢視 > 成績作業
- [x] 其他選單、功能沒有被改動

---

## 修改記錄

- 2026-02-26：功能入口調整
  - 註解移除入口：檢查班級群科班設定
  - 註解移除入口：開課檢核課程代碼
  - 註解移除入口：學期成績檢核課程代碼
  - 路徑調整：修課檢核課程代碼 → 教務作業 > 批次作業/檢視 > 成績作業

---

## 注意事項

1. **功能保留**：被註解的功能程式碼仍保留在檔案中，可於未來需要時恢復
2. **不影響功能**：僅移除選單入口，功能本身的程式碼（Form、Business Logic）未修改
3. **路徑一致性**：確保新路徑層級符合需求規格
4. **向後相容**：其他功能不受影響，僅調整指定功能的顯示位置
