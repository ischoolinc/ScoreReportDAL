# Todo.md — 動態調整樞紐分析來源（Named Range 方案，0304）

## 目標
在 `frmCheckSCAttendCourseCode.cs` 中，依據資料實際筆數動態更新 Excel 樣板內 PivotTable 的資料來源。

**前提**：你已在樣板 `修課檢核課程代碼樣板.xlsx` 完成一次性設定：
- 已建立 Named Range：`ptSrc_CheckSCAttendCourseCode`
- PivotTable 的「變更資料來源」已改為 `ptSrc_CheckSCAttendCourseCode`

完成修改後，請將變更記錄到：`修課檢核課程代碼調整0304.md`

---

## 範圍與限制
- **只修改**：`frmCheckSCAttendCourseCode.cs`
- 不變更樞紐分析欄位配置、格式、圖表、工作表名稱、欄位標題。
- 資料工作表：`檢查修課學生課程代碼`（Worksheet 變數：`wstSC`）
- 樞紐分析工作表：`檢查修課學生課程代碼_統計`（Worksheet 變數：`wsStat`）
- 欄位固定 A~U（21 欄）。

---

## 背景
舊版 Aspose.Cells 在樣板已存在 PivotCache 的情況下，`PivotTable.ChangeDataSource(...)` 常無法可靠更新 PivotCache 的 `worksheetSource ref`，導致 Pivot 來源範圍看似未更新、出現 Blank。

改用 Named Range 方案後：
- PivotTable 永遠指向 `ptSrc_CheckSCAttendCourseCode`
- 程式每次只更新 `Name.RefersTo` 指向 `='檢查修課學生課程代碼'!A1:U{endRow}`

此方式最穩、維護成本最低。

---

## 任務清單

### 1) 確認/保留「以 A 欄判斷最後資料列」方法
若目前已存在 `GetLastDataRowByColumnA(Worksheet ws, int headerRowIndex = 0)`，保留並使用。
若沒有，新增該方法：
- 從 `ws.Cells.MaxDataRow` 開始往上找
- 以 A 欄（col=0）的 `StringValue` 判斷是否空白
- 找到最後一列有值即回傳 rowIndex（0-based）
- 若只剩 header，回傳 `headerRowIndex` 或 -1（呼叫端判斷 return）

### 2) 寫入資料後，動態更新 Named Range 的 RefersTo
在「資料已完整寫入 wstSC」之後、Pivot Refresh 之前，加入更新 NamedRange 的程式碼：

- Named Range 名稱固定：`ptSrc_CheckSCAttendCourseCode`
- RefersTo 格式：`='{wstSC.Name}'!A1:U{endRow}`
- endRow 使用任務 1 的 lastRow 計算：
  - `lastRow = GetLastDataRowByColumnA(wstSC, headerRowIndex: 0)`
  - 若 `lastRow < 1`（只有標題或無資料）則 return
  - `endRow = lastRow + 1`（轉成 1-based）

**實作要點（請照做）：**
- 若 name 不存在：`_wb.Worksheets.Names.Add(name)`
- 若已存在：直接更新 `RefersTo`
- 不要 Remove 再 Add（避免破壞 Pivot 指向該 name 的關聯）

### 3) PivotTable Refresh / Calculate（保持既有行為）
在更新 NamedRange 後，保留你現有刷新樞紐的做法：

- `pt.RefreshData();`
- `pt.CalculateData();`
- `DisableSubtotalsForField(pt, "部定校訂");`

> 注意：不再依賴 `pt.ChangeDataSource(...)` 來變更資料來源（Pivot 來源固定指向 named range）。

### 4) 防呆與註解
- 若 `lastRow < 1`：直接 return（避免 Pivot 指到空範圍）
- 在 NamedRange 更新程式碼旁加註解：
  - Pivot 的資料來源使用 Named Range：`ptSrc_CheckSCAttendCourseCode`
  - 每次輸出只更新 `RefersTo` 即可動態調整 Pivot 來源範圍

---

## 建議插入位置（frmCheckSCAttendCourseCode.cs）
請搜尋並定位：
- `Worksheet wstSC = _wb.Worksheets["檢查修課學生課程代碼"];`
- 寫入資料到 `wstSC.Cells[...]`
- `Worksheet wsStat = _wb.Worksheets["檢查修課學生課程代碼_統計"];`
- `foreach (PivotTable pt in wsStat.PivotTables) { ... RefreshData/CalculateData ... }`

**插入點：**
- 放在 `wsStat.PivotTables` 迴圈之前（Refresh 之前），且確保 wstSC 資料已寫完。

---

## 參考程式碼片段（請按實際變數名稱整合）

```csharp
// === 動態更新 Pivot 來源（Named Range） ===
const string PivotSourceName = "ptSrc_CheckSCAttendCourseCode";

// 以 A 欄判斷最後資料列，避免 MaxDataRow 受格式/空白列影響
int lastRow = GetLastDataRowByColumnA(wstSC, headerRowIndex: 0);
if (lastRow < 1)
    return;

// 轉成 Excel 1-based 行號；固定欄位 A~U
int endRow = lastRow + 1;
string refersTo = $"='{wstSC.Name}'!A1:U{endRow}";

// 取得/建立 Named Range 後更新 RefersTo（不要 Remove/重建，避免破壞 Pivot 指向）
int nameIdx = _wb.Worksheets.Names.GetIndex(PivotSourceName);
if (nameIdx == -1)
    nameIdx = _wb.Worksheets.Names.Add(PivotSourceName);

_wb.Worksheets.Names[nameIdx].RefersTo = refersTo;

// === 刷新 Pivot ===
Worksheet wsStat = _wb.Worksheets["檢查修課學生課程代碼_統計"];
foreach (PivotTable pt in wsStat.PivotTables)
{
    pt.RefreshData();
    pt.CalculateData();
    DisableSubtotalsForField(pt, "部定校訂");
}
```

---

## 驗收標準（Acceptance Criteria）
1. 輸出 Excel 中 PivotTable 的資料來源為 **Named Range**：`ptSrc_CheckSCAttendCourseCode`
2. `ptSrc_CheckSCAttendCourseCode` 的 RefersTo 在輸出檔內正確指向：
   - `='檢查修課學生課程代碼'!A1:U{實際最後列}`
3. PivotTable 結果會依據新增/減少資料筆數變動（不再固定整張表）。
4. 原本 `DisableSubtotalsForField(pt, "部定校訂")` 行為不變。
5. 編譯成功、產生報表流程不受影響。

---

## 變更紀錄（必填）
請在 `修課檢核課程代碼調整0304.md` 新增紀錄，至少包含：
- 修改檔案：frmCheckSCAttendCourseCode.cs
- 變更摘要：
  - Pivot 來源改採 Named Range（ptSrc_CheckSCAttendCourseCode）
  - 程式動態更新 Names.RefersTo 指向 A1:U{endRow}
  - Pivot Refresh/Calculate 維持
- 驗證方式與結果（簡述）

